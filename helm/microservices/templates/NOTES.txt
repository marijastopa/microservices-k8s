Microservices application deployed successfully.

{{ len .Values.services }} services are now running.

{{- if eq (index .Values.services "frontend").type "LoadBalancer" }}

Get the frontend URL:

  export FRONTEND_IP=$(kubectl get svc frontend -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
  echo http://$FRONTEND_IP

{{- else }}

Port-forward to access frontend locally:

  kubectl port-forward svc/frontend 8080:80

Then open http://localhost:8080

{{- end }}

Check deployment status:

  kubectl get pods
  kubectl get hpa

{{- if not .Values.redis.enabled }}

Note: Redis is not enabled. Cart service uses in-memory storage.
To enable Redis, run:

  helm upgrade {{ .Release.Name }} . --set redis.enabled=true

{{- end }}